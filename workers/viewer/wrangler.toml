name = "geolibre-viewer"
main = "src/index.ts"
compatibility_date = "2025-03-01"

# Custom domain (optional). Uncomment and update if you own a Cloudflare-managed
# domain. Otherwise the worker is served at:
#   https://geolibre-viewer.<your-account>.workers.dev
#
# [[routes]]
# pattern = "viewer.yourdomain.app"
# custom_domain = true
